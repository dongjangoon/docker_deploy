version: "3"
services:
  proxy:
    image: nginx:latest
    container_name: proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./proxy/nginx/:/etc/nginx

  express1:
    image: backend:latest
    container_name: express1
    ports:
      - "8081:8081"
